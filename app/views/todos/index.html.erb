<ul class="todos">
  <% @todos.each do |todo| %>
    <li class="<%= "completed" if todo.completed? %>">
      <%= todo.title %>
      <%= render_completion_button todo %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a new todo", new_todo_path %>
